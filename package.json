{
  "name": "neuro-chord",
  "private": true,
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "start:dev": "nest start --watch "
  },
  "workspaces": [
    "apps/*"
  ],
  "overrides": {
    "@types/pg": "8.11.11"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8"
  }
}
